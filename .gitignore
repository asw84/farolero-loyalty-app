# =========================================================================
# Универсальный .gitignore для Full-Stack JavaScript проекта
# =========================================================================

# --- Зависимости ---
# Никогда не добавляйте эту папку в Git. Она может весить сотни мегабайт
# и полностью пересоздается командой `npm install` или `yarn install`.
# ЭТО ГЛАВНАЯ ПРИЧИНА ВАШЕЙ ОШИБКИ "Exec format error".
/node_modules

# --- Секреты и Конфигурация ---
# Содержит секретные ключи, пароли, токены. Огромный риск безопасности.
# Вместо этого, в репозиторий можно добавить файл .env.example с примерами переменных.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
/tokens.json

# --- Логи ---
# Файлы логов и отладки, которые генерируются во время работы приложения.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Сборочные артефакты (Build output) ---
# Папки, которые создаются автоматически после сборки фронтенда или бэкенда.
/build
/dist
.next/
.nuxt/
.out/

# --- Файлы Базы Данных ---
# Локальные файлы базы данных, такие как SQLite. Это данные, а не код.
*.db
*.sqlite
*.sqlite3

# --- Файлы, специфичные для ОС ---
# Системный "мусор" от macOS, Windows и Linux.
.DS_Store
._*
Thumbs.db
desktop.ini
*.swp

# --- Файлы IDE и редакторов кода ---
# Персональные настройки вашей среды разработки. Они не нужны другим.
.idea/
.vscode/
*.sublime-project
*.sublime-workspace

# --- Отчеты о покрытии тестами ---
# Генерируются при запуске тестов и не являются частью исходного кода.
/coverage
.jest-cache

# --- Временные файлы и архивы ---
.tmp/
*.zip
*.tar
*.gzbackend/.env
backend/tokens.json
amocrm*.json
**/token.json
.sqlite
*.db
